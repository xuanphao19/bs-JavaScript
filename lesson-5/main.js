var thutrang = "ha trang"; // logic ..... MOng muốn trả 10
var boTao = "Thanh Hòa"; // logic ..... MOng muốn trả 10
// const userInput = prompt("Nhập giá trị:");
// console.log("Giá trị nhập:", userInput);

// Toán tử tăng giảm: (--) or (++) :
//⭐ Tăng hoặc giảm một đơn vị
//⭐ - Gán kết quả tt cho biến gốc.
//🔴 khi nằm ở tiền tố làm: Trả về kết quả sau tính toán
//🔴 khi nằm ở hậu tố làm: Trả về giá trị copy

var a = 1;
var b = 2;
var c = "d";

var B = "10" + (a - 1);

0;
("");
false;
null;
undefined;
NaN;

var result = 0 && a > b && a > b;

if ("" || a > b || a > b) {
  console.log(result, 123);
}
